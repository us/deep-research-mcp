OPENAI_API_KEY=  # Your OpenAI API key
OPENAI_ENDPOINT= # Optional: Custom OpenAI-compatible endpoint 
FIRECRAWL_KEY=   # Your Firecrawl API key (not needed if using local instance)

# Firecrawl Configuration
FIRECRAWL_CONCURRENCY=2      # Number of concurrent searches (default: 2)
FIRECRAWL_BASE_URL=        # Local Firecrawl or CRW URL (e.g. "http://localhost:3002" or "https://fastcrw.com")

# OpenAI Configuration
CONTEXT_SIZE="128000"
# If you want to use other OpenAI compatible API, add the following below:
# OPENAI_ENDPOINT="http://localhost:11434/v1"
# OPENAI_MODEL="llama3.1"


# Observability (Optional)
LANGFUSE_PUBLIC_KEY=
LANGFUSE_SECRET_KEY=
LANGFUSE_BASEURL=